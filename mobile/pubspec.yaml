name: smart_patient_alert
description: Flutter mobile caregiver app for Smart Patient Gesture Communication System.
publish_to: "none"
version: 2.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_tts: ^4.0.2

flutter:
  uses-material-design: true
